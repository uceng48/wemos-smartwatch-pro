<?xml version='1.0' encoding='utf-8'?>
<widget id="com.wemos.alarm" version="1.0.0" xmlns="http://www.w3.org/ns/widgets" xmlns:cdv="http://cordova.apache.org/ns/1.0">
    <name>Wemos Alarm</name>
    <description>Aplikasi monitoring Wemos dengan alarm lokal.</description>
    <author email="dev@wemos.com" href="http://wemos.com">
        Wemos Team
    </author>
    
    <!-- Konten utama aplikasi -->
    <content src="index.html" />
    
    <!-- Akses ke semua jaringan (untuk WebSocket) -->
    <access origin="*" />
    <allow-navigation href="*" />
    <allow-intent href="http://*/*" />
    <allow-intent href="https://*/*" />

    <!-- Preferensi Platform Android -->
    <platform name="android">
        <!-- Versi SDK -->
        <preference name="android-minSdkVersion" value="26" />
        <preference name="android-targetSdkVersion" value="34" />
        
        <!-- Menjaga aplikasi tetap berjalan saat di-background -->
        <preference name="KeepRunning" value="true" />
        
        <!-- Izin (Permissions) -->
        <config-file target="AndroidManifest.xml" parent="/manifest">
            <uses-permission android:name="android.permission.INTERNET" />
            <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
            <uses-permission android:name="android.permission.WAKE_LOCK" />
            <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
            <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
            <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
            <uses-permission android:name="android.permission.VIBRATE" />
            <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
        </config-file>

        <!-- Konfigurasi Service Background Mode -->
        <config-file target="AndroidManifest.xml" parent="/manifest/application">
            <!-- Perbaikan nama class service menjadi 'de.appplant.cordova.plugin.background.BackgroundService' -->
            <service 
                android:name="de.appplant.cordova.plugin.background.BackgroundService" 
                android:foregroundServiceType="dataSync"
                android:exported="false" />
            
            <!-- Meta data untuk kompatibilitas -->
            <meta-data android:name="android.supports_rtl" android:value="true" />
        </config-file>
    </platform>
</widget>
