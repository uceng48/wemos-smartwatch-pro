<!-- ===== PLATFORM ANDROID ===== -->
<platform name="android">
    
    <!-- SDK: target 34 (Android 14), min 24 (Android 7) -->
    <preference name="android-minSdkVersion" value="24" />
    <preference name="android-targetSdkVersion" value="34" />
    
    <!-- Foreground service tipe untuk background mode (wajib API 34+) -->
    <preference name="AndroidForegroundServiceType" value="dataSync" />
    
    <!-- Izinkan traffic HTTP (cleartext) -->
    <edit-config file="app/src/main/AndroidManifest.xml" mode="merge" target="/manifest/application">
        <application android:usesCleartextTraffic="true" />
    </edit-config>
    <preference name="AndroidInsecureFileModeEnabled" value="true" />
    
    <!-- ===== KOMPILASI JAVA 17 ===== -->
    <edit-config file="app/build.gradle" mode="merge" target="/project">
        <compileOptions>
            <sourceCompatibility>17</sourceCompatibility>
            <targetCompatibility>17</targetCompatibility>
        </compileOptions>
    </edit-config>
    
    <!-- ===== IZIN ANDROID (lengkap untuk API 34) ===== -->
    <config-file parent="/manifest" target="AndroidManifest.xml">
        <!-- Izin untuk Android 13+ (notifikasi) -->
        <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
        <!-- Izin umum -->
        <uses-permission android:name="android.permission.VIBRATE" />
        <uses-permission android:name="android.permission.WAKE_LOCK" />
        <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
        <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
        <uses-permission android:name="android.permission.INTERNET" />
        <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
        <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
        <!-- Izin untuk alarm eksak (API 31+) -->
        <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
        <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    </config-file>
    
    <!-- ===== DEKLARASI FOREGROUND SERVICE ===== -->
    <config-file parent="/manifest/application" target="AndroidManifest.xml">
        <service 
            android:name="com.katason.backgroundmode.BackgroundService" 
            android:foregroundServiceType="dataSync"
            android:exported="false" />
        <meta-data android:name="android.supports_rtl" android:value="true" />
    </config-file>
    
    <allow-intent href="market:*" />
</platform>
